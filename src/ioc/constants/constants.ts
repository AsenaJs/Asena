export const DependencyKey = '_dependencies';

export const ScopeKey = '_scope';

export const NameKey = '_name';

export const StrategyKey = '_strategy';

export const InterfaceKey = '_interface';

export const PathKey = '_path';

export const MiddlewaresKey = '_middlewares';

export const IsMiddlewareKey = '_isMiddleware';

export const CronKey = '_cron';

export const IOCObjectKey = '_iocObject';
